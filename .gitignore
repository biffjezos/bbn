# Ruby / Jekyll
ui/vendor/
ui/.jekyll-cache/
ui/_site/
public/

# Node / services — not deployed via CI, managed separately (Railway)
services/node_modules/
services/*.lockb
services/.env

# Rust build output
services/target/

# Root-level env files — NEVER commit secrets
.env
.env.local
*.log

# Claude Code local settings (may contain secrets)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
