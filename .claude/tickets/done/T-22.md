---
id: T-22
title: Security Hardening & Capacity Tuning
status: done
completed: 2026-03-23
branch: claude/review-next-tasks-Ag7D8
closes: [SEC-1.2, SEC-1.3, SEC-1.4, SEC-1.5, SEC-1.6]
---

✅ Implemented 2026-03-23. Also implements T-16 Phase 1 partial: `admin_settings` collection + admin Settings tab.

## What was done

- **common/src/auth.rs**: added `ttl_secs: Option<u64>` to `UserTokenParams`.
- **migration-service**: migration `009_admin_settings` — creates `admin_settings` collection seeded with 16 default values (rate limits, JWT TTL, message TTL, body limit, etc.).
- **users-service**: `GET /admin/settings`, `PUT /admin/settings/:key`, `GET /internal/settings`. JWT TTL cached in `AppState`, refreshed every 60 s.
- **auth-service**: `user_jwt_ttl_secs` in `AppState`, refreshed every 60 s. Both token-issuing call sites pass `ttl_secs`.
- **messages-service**: `MsgSettings` struct loaded from `admin_settings`, refreshed every 60 s. Per-userId `UserBuckets` added. Dynamic `max_chars` and TTL replace hardcoded constants.
- **gateway**: `real_ip()` checks `cf-connecting-ip` first (SEC-1.3). `LiveLimiter` — all 5 rate limiters swappable at runtime. `lim_msg` dedicated rate limiter (SEC-1.6). `DefaultBodyLimit` layer (SEC-1.5). Background task refreshes all 5 limiters every 60 s.
- **ui/scripts/api.js**: `adminGetSettings()` and `adminUpdateSetting(key, value)` added.
- **ui/scripts/admin.js**: "Settings" tab with editable inputs, Save buttons, description tooltips.
