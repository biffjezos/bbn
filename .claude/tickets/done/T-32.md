---
id: T-32
title: Profile pages — wire profile.js into boomboom.js
status: done
priority: medium
concern: ui
phase: 2/2
completed: 2026-03-30
---

## What Was Done

- `ui/scripts/profile.js` converted to ES6 module at `ui/scripts/lib/profile.js`
- All `window.Api.*` → `Api` (imported from `./api.js`)
- All `window.Auth.*` → `Auth` (imported from `./auth.js`)
- `window.BlockModule?.prompt()` → `promptBlock` (imported from `./blocks.js`)
- Self-executing init replaced with exported `initMyProfile()` and `initPublicProfile()`
- `api.js`: added `getMyVenues`, `createVenue`, `updateVenue`, `deleteVenue`
- `boomboom.js`: imports `initMyProfile`, `initPublicProfile`; calls them after `await window.__authReady` when the relevant element exists
- Old `ui/scripts/profile.js` stubbed out with a redirect comment
