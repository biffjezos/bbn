---
id: T-29
title: JS cleanup — fix ES6 module refactor, trim to UI-only
status: done
priority: high
concern: ui
phase: 3/3
completed: 2026-03-30
---

## Summary

Fixed all JS breakage introduced by the ES6 module refactor and removed all
Jekyll-era dead paths. The site now loads without console errors.

## Phase 1 — Audit (completed inline before Phase 2)

Classified all modules in `ui/scripts/lib/`. Root cause confirmed: ES6
refactoring exported named modules but never assigned them to `window.*`,
while all inter-module calls still used `window.Api`, `window.Auth`, etc.

## Phase 2 — Fix broken ES6 module errors

- `auth.js`: added `import { Api } from './api.js'`; replaced all `window.Api.*` with `Api.*`; `window.BBMCrypto` → `window.BBNCrypto`
- `api.js`: `API_BASE = '/api'` (was `''`); added `getNotifications` / `dismissNotification`
- `warmup.js`: `window.BOOMBOOM_API_URL + '/health'` → `fetch('/api/health')`
- `geo.js`: imported `Api`; `locWsUrl()` uses `location.origin`; `window.Api.*` → `Api.*`
- `messages.js`: imported `Api`; `msgWsUrl()` uses `location.origin`; `window.Api.*` → `Api.*`
- `notifications.js`: removed `window.BOOMBOOM_BASE` path prefix; removed undefined `DEBUG` ref
- `boomboom.js`: imported `OpaqueClient`; set `window.Auth/Api/OpaqueClient` in `initApp()`; moved `initNotifications()` after `wireAuth()`; SW path fixed to `/service-worker.js` scope `/`; `const BASE = ''`

## Phase 3 — Dead code removal and simplification

- `favourites.js`: `window.BOOMBOOM_BASE || ''` → `''`
- `profile.js`: `window.BOOMBOOM_BASE || ''` → `''`; `window.BBMCrypto` → `window.BBNCrypto`
- `settings.js`: fully restored (all function bodies were stubs)
- `boomboom.js`: `initSettings()` moved after `await window.__authReady`
- `crypto.js`: has `window.BOOMBOOM_BASE || ''` — untouchable per project rules; evaluates to `''` which is correct

## Remaining (new ticket T-32)

`profile.js` still not loaded by any template — profile pages show "Loading…". T-32 created to convert it to ES6 module and wire into boomboom.js.
