---
id: T-20
title: Sharded Location Store (performance at scale)
status: deferred
priority: low
concern: db
phase: 4/5
---

Phases 1–4 ✅ complete (2026-03-23). Details in `tickets/done/T-20-phases1-4.md`.

---

## Phase 5 — Auto-adjustable shard size (deferred)

Track here, implement later.

**Idea:** A background task monitors shard population sizes. If any shard exceeds a configurable `SHARD_MAX_USERS` threshold, halve `SHARD_SIZE_M` for the next startup (write to a `_meta` doc). If all shards fall below `SHARD_MIN_USERS`, double it. Requires graceful re-bucketing logic.

Low priority until real load data exists.
